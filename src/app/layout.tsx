import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Voxelbox - AI-Native Software Development Platform',
  description: 'Generate production-ready applications for businesses with AI-powered development, Square integration, and automated business formation.',
  keywords: ['AI development', 'business formation', 'Square integration', 'automated development'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
}